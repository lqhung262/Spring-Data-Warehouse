package com.example.demo.controller.systemuser;

// Admin debug removed per request. This class intentionally left blank to avoid exposing debug endpoints.
public class AdminDebugController {
    // debug endpoint removed
}
